# Toil 言語処理系 執筆コンテキスト

本書「育てて学ぶはじめての自作プログラミング言語」の全体像、方針、Toil 言語の仕様に関する前提をまとめたコンテキストファイルです。

## 1. 現在の進捗と次回のタスク（随時更新）
* **現在の進捗**: 
  * 第 2 部導入（`020000`）〜 4.10 節 タプル（`020410`）までのレビューが完了。
  * `020410`（タプル）でフロントマターの全角スペース統一、本文中の「4 つめ」などの表現の整理、リストと配列の用語方針の確認を実施。
  * `020406`（elif）で `else 節` などのキーワード小文字表記統一や、実行例の網羅性を確認済み。
  * `020407`（return）で `try-except` への用語統一や「例外を送出する」といった Python 公式用語への準拠を実施。
  * `020408`（break・continue）で、構文の概念（「while 式」「try 文」）とキーワード（`while`）のバッククォート表記の使い分けを整理。
  * `020409`（while-then-else）で diff のクラス名誤記や言語指定を修正し、章末にコレクションへの「ヒキ」を追加。第 2 部冒頭 (`020000`) の概要も新しい順序に合わせて更新済み。
  * `020402`（型ヒント）に `from __future__ import annotations` の導入と `Environment.__init__()` の型ヒントからの引用符除去を反映。本文と脚注で「評価の遅延/回避」の違いを丁寧に説明済み。
  * `020401`（Ident）で「プロパティ」→「属性」の修正、特殊メソッドのカッコ表記統一を実施。
* **次回の予定**: 
  * コレクションのうち「配列」の実装・執筆から再開する。
  * 読者の学習負荷を分散させるため、引き続き以下のスモールステップ順で進める：
    1. 配列（配列リテラル構文 `[]` のパース）
    2. 配列（インデックスへの代入[更新]）
    3. 文字列（エスケープシーケンスなし）
    4. 文字列（エスケープシーケンスあり）

## 1. 書籍の基本方針とターゲット読者

* **ターゲット**: Python の基礎（リスト、辞書、クラス）は知っているが、言語処理系は初めての読者。
* **基本方針**: 厳密さよりも「楽しさ」「わかりやすさ」を優先。一度に多くの概念を詰め込まず、少しずつ機能を追加して動かすことで達成感を持たせる。
* **トーン＆マナー（ニュアンス）**:
  * 親しみやすい語り口を維持する（例：「〜ではないかと」「ここでお詫びがあります」のような、読者に語りかけるようなユーモアや引きを大切にする）。
  * 各部や各章の終わりでは、次へのモチベーションを高める「引き」を必ず入れる。
  * 専門用語は標準的なものを使いつつ、発展的な学習への橋渡しとなる豆知識（注釈やコラム）を適宜挟む。

## 2. 全体の目次構成とあらすじ（現時点）

* **第1部**: 機能を最小限に絞り、処理系の原理を示す。ツリーウォーク式と中間コード式（スタックマシン、コンパイラ）の両方を作成する。
* **第2部**: ツリーウォーク式インタプリタに戻り、トイ言語として「普通にプログラムが書ける」レベルまで機能拡張する。
  * **第4章（基本のインタプリタ）**: 識別子クラス・型ヒントの下準備から始め、比較/単項演算子、`and`/`or`、`elif`、`return`、`break`/`continue`、配列、文字列、タプル等を順次追加。
  * **第5章（辞書）**: 辞書型の導入。ドット記法によるパイプライン的・メソッド的記述。
  * **第6章（パターンマッチ）**: 可変長引数、分割代入、`match` 文、アロー関数などを実装。
* **第5部**: 再び中間コード式インタプリタに戻り、拡張を行う。

## 3. Toil言語の仕様・アーキテクチャ上の割り切り
* **Pythonネイティブへの依存**: 数値（`int`）や真偽値（`bool`）など、Python の値をそのまま Toil の値として流用することで実装をシンプルに保つ。
* **AST上の工夫（`Ident` クラス）**: 今後文字列型を導入するにあたり、AST 上の「変数名」と「文字列データ」の区別がつかなくなるのを防ぐため、内部実装として専用の `Ident` クラスを導入する。
* **構文糖衣（シンタックスシュガー）**: 例えば `def` は構文解析の時点で `define` と `func` に変換するなど、評価器を複雑にしない工夫をしている。
* コレクションの要素アクセス `t[2]` も、構文解析の時点で組み込み関数 `index(t, 2)` の呼び出しに変換（デシュガー）することで、評価器の負担を減らしている。
* **割り切り（トレードオフ）**:
  * 「正しいソースコードが入力されれば正しく動くこと」を最優先とする。
  * エラー処理、堅牢性、性能チューニング、メモリ管理については深追いせず、必要に応じて読者に注意喚起するにとどめる。
  * （例）`break` や `continue` が関数を超えて大域脱出する（動的スコープ的に捕捉される）挙動についても、エラーチェックを複雑にしないための「裏技」として許容する。