# Toil 言語処理系 執筆コンテキスト

本書「育てて学ぶはじめての自作プログラミング言語」の全体像、方針、Toil 言語の仕様に関する前提をまとめたコンテキストファイルです。

## 1. 現在の進捗と次回のタスク（随時更新）
* **現在の進捗**: 
  * 第 2 部導入（`020000`）〜 4.11 節 配列（`020411`）までのレビュー・修正が完了。
  * 4.1〜4.9 節: 識別子クラス・型ヒント・各種演算子・制御構造（elif, return, break/continue, while-then-else）の整備完了。
  * 4.10 節（タプル）: 組み込み関数・構文糖衣（`[]`）の導入、二分探索木の参照先明示、数え方表記（「3 つめ」等）の統一。
  * 4.11 節（配列）: 組み込み関数（`list`, `push`, `pop`）と配列リテラル（`[]`）の構文解析・評価器対応、型定義（`Expr`, `Value`）の更新、脚注 `[^not-array]` の追加、コード例網羅。
* **次回の予定**: 
  * 配列の「インデックスへの代入（要素の更新）」の実装・執筆。
  * スモールステップ順：①配列リテラル[済] → ②インデックス代入[次回] → ③文字列（エスケープなし） → ④文字列（エスケープあり）

## 2. 書籍の基本方針とターゲット読者
* **ターゲット**: Python の基礎（リスト、辞書、クラス）は知っているが、言語処理系は初めての読者。
* **基本方針**: 厳密さよりも「楽しさ」「わかりやすさ」を優先。一度に多くの概念を詰め込まず、スモールステップで動かして達成感を持たせる。
* **トーン＆マナー**:
  * 親しみやすい語り口（読者への語りかけやユーモア）を維持する。
  * 各部・各章・各節の終わりには、次へのモチベーションを高める「ヒキ」を必ず入れる。
  * 専門用語は標準的なものを用いつつ、発展的な学習への橋渡しとなる注釈やコラムを適宜挟む。

## 3. 全体の目次構成とあらすじ（現時点）
* **第 1 部**: 機能を最小限に絞り、処理系の原理を示す（ツリーウォーク式・中間コード式）。
* **第 2 部**: ツリーウォーク式に戻り、トイ言語として実用的な機能まで拡張。
  * **第 4 章（基本のインタプリタ）**: 識別子・型ヒント、比較/単項演算子、`and`/`or`、`elif`、`return`、`break`/`continue`、コレクション（タプル、配列、文字列等）を順次追加。
  * **第 5 章（辞書）**: 辞書型の導入。ドット記法によるパイプライン的記述。
  * **第 6 章（パターンマッチ）**: 可変長引数、分割代入、`match` 文、アロー関数等。
* **第 5 部**: 再び中間コード式インタプリタに戻り、拡張を行う。

## 4. Toil 言語の仕様・アーキテクチャ上の割り切り
* **Python ネイティブへの依存**: 数値・真偽値など、Python の値をそのまま Toil の値として流用。
* **AST 上の工夫（`Ident` クラス）**: 変数名と文字列データの混同を防ぐため、専用の `Ident` クラスを使用。
* **コレクションの AST 表現と構文糖衣**:
  * タプル: AST の式タグ自体に Python のタプルを使っているため、Toil のタプルは関数呼び出し形式の AST（`("tuple", [...])`）として扱う。
  * 配列: AST の式タグに使われていないため、AST 上でも Python の `list` をそのままリテラル表現として流用。
  * 要素アクセス: `t[2]` は構文解析時に組み込み関数 `index(t, 2)` の呼び出しに変換（デシュガー）。
  * 関数定義: `def` も構文解析時に `define` と `func` にデシュガー。
* **割り切り（トレードオフ）**:
  * 「正しいソースコードが入力されれば正しく動くこと」を最優先とし、エラー処理・堅牢性・性能チューニングは深追いしない（必要に応じた注意喚起にとどめる）。
  * `break` / `continue` が関数を超えて大域脱出する挙動なども、実装をシンプルに保つための割り切りとして許容。