---
title: "3.12 いろいろ書いてみる（再々）"
---

2.19 とまったく同じ例です。実行の方式はまったく異なりますが結果は同じ。

```py
    print("Factorial:")

    toil.run(r"""
        def factorial_iter(n) do
            result := 1;
            while n > 0 do
                result = result * n;
                n = n - 1
            end;
            result
        end
    """)
    print(toil.run(r""" factorial_iter(0) """))
    # -> 1
    print(toil.run(r""" factorial_iter(1) """))
    # -> 1
    print(toil.run(r""" factorial_iter(4) """))
    # -> 24

    toil.run(r"""
        def factorial_rec(n) do
            if n == 0 then 1 else n * factorial_rec(n - 1) end
        end
    """)
    print(toil.run(r""" factorial_rec(0) """))
    # -> 1
    print(toil.run(r""" factorial_rec(1) """))
    # -> 1
    print(toil.run(r""" factorial_rec(4) """))
    # -> 24

    print("Fibonacci:")

    toil.run(r"""
        def fib_iter(n) do
            a := 0; b := 1;
            while n > 0 do
                tmp := b; b = a + b; a = tmp;
                n = n - 1
            end;
            a
        end
    """)
    print(toil.run(r""" fib_iter(0) """))
    # -> 0
    print(toil.run(r""" fib_iter(1) """))
    # -> 1
    print(toil.run(r""" fib_iter(6) """))
    # -> 8

    toil.run(r"""
        def fib_rec(n) do
            if n == 0 then 0
            else if n == 1 then 1
            else fib_rec(n - 1) + fib_rec(n - 2) end end
        end
    """)
    print(toil.run(r""" fib_rec(0) """))
    # -> 0
    print(toil.run(r""" fib_rec(1) """))
    # -> 1
    print(toil.run(r""" fib_rec(6) """))
    # -> 8

    print("GCD:")

    toil.run(r"""
        def gcd_iter(a, b) do
            while b > 0 do
                tmp := b; b = a % b; a = tmp
            end;
            a
        end
    """)
    print(toil.run(r""" gcd_iter(12, 18) """))
    # -> 6

    toil.run(r"""
        def gcd_rec(a, b) do
            if b == 0 then a else gcd_rec(b, a % b) end
        end
    """)
    print(toil.run(r""" gcd_rec(12, 18) """))
    # -> 6

    print("Even/Odd (Mutual Recursion):")

    toil.run(r"""
        def even(n) do if n == 0 then True else odd(n - 1) end end;
        def odd(n) do if n == 0 then False else even(n - 1) end end
    """)
    print(toil.run(r""" even(2) """))
    # -> True
    print(toil.run(r""" even(3) """))
    # -> False
    print(toil.run(r""" odd(2) """))
    # -> False
    print(toil.run(r""" odd(3) """))
    # -> True

    print("Counter (Closure):")

    toil.run(r"""
        def make_counter() do
            count := 0;
            func do count = count + 1 end
        end
    """)
    toil.run(r""" c1 := make_counter() """)
    toil.run(r""" c2 := make_counter() """)
    print(toil.run(r""" c1() """))
    # -> 1
    print(toil.run(r""" c1() """))
    # -> 2
    print(toil.run(r""" c2() """))
    # -> 1
    print(toil.run(r""" c2() """))
    # -> 2

    print("Binary search tree:")
    print("Building tree:")

    toil.run(r"""
        def node(val, left, right) do
            func op do
                if op == 1 then val
                else if op == 2 then left
                else right end end
            end
        end
    """)

    toil.run(r""" n1 := node(2, 3, 4) """)
    print(toil.run(r""" n1(1) """))
    print(toil.run(r""" n1(2) """))
    print(toil.run(r""" n1(3) """))
    # -> 2\n3\n4

    toil.run(r"""
        def bst_put(bst, val) do
            if bst == None then node(val, None, None)
            else
                cur_val := bst(1);
                if val == cur_val then
                    bst
                else if val < cur_val then
                    node(cur_val, bst_put(bst(2), val), bst(3))
                else
                    node(cur_val, bst(2), bst_put(bst(3), val))
                end end
            end
        end
    """)
    toil.run(r""" bst := None """)
    toil.run(r""" bst = bst_put(bst, 7) """)
    toil.run(r""" bst = bst_put(bst, 3) """)
    toil.run(r""" bst = bst_put(bst, 1) """)
    toil.run(r""" bst = bst_put(bst, 9) """)
    toil.run(r""" bst = bst_put(bst, 5) """)

    print("Walking tree:")
    toil.run(r"""
        def bst_walk(bst) do
            if bst == None then None
            else
                bst_walk(bst(2)); print(bst(1)); bst_walk(bst(3))
            end
        end
    """)
    toil.run(r"""
        def bst_find(bst, val) do
            if bst == None then False
            else
                cur_val := bst(1);
                if val == cur_val then val
                else if val < cur_val then bst_find(bst(2), val)
                else bst_find(bst(3), val)
                end end
            end
        end
    """)

    toil.run(r""" bst_walk(bst) """)
    # -> 1\n3\n5\n7\n9

    print("Finding values:")
    toil.run(r"""
        i := 0;
        while i < 10 do
            print(bst_find(bst, i));
            i = i + 1
        end
    """)
    # -> False\n1\nFalse\n3\nFalse\n5\nFalse\n7\nFalse\n9
```

そういえば、`def` は作っていないのに使えてますね。これは `def` が構文糖衣で、構文解析器により `define` と `func` に変換されるためです。

これですべての機能がカバーできました！中間コード式インタプリタ、いかがだったでしょうか？

ソース：https://github.com/koba925/toil-book/blob/0312_examples/toil.py
差分：https://github.com/koba925/toil-book/compare/0311_user_functions...0312_examples

## 3 章のまとめ

第 3 章では第 2 章の字句解析・構文解析器をそのまま使い、バックエンドを中間コードへのコンパイラ＋仮想マシンに入れ替えました。とても小さいコンパイラと仮想マシンですが、コンパイラ・スタックマシン・バックパッチ・制御スタックなど、学んだことはたくさんあります。

## 第 1 部のまとめ

第 1 部では、機能を最小限に絞り、ツリーウォーク式・中間コード式の 2 通りの方法で小さなインタプリタを作成しました。字句解析器・構文解析器・評価器・コンパイラ・仮想マシン・環境と、独立したパーツに分けることにより、組み合わせて全体を作ったり、パーツの組み合わせを変えて実行方式を変えたりできることも学びました。再帰呼び出しや再帰下降構文解析、スタックマシン、バックパッチなどの重要な手法も身についているはずです。

処理系のプログラムはツリーウォーク式も中間コード式も両方入ってまだ 500 行足らずで、まだまだ上から下まで全部読むというのが無理なくできる大きさです。細かいステップに分けて進めてきましたので、一度全体を読み直してみると全体像がよりいっそう理解が深まると思います。

ここまでの内容が理解できれば自作プログラミング言語の入門は卒業といってよいでしょう。第 2 部はツリーウォーク式インタプリタに戻っていろいろ機能を拡張していきますが、ある意味ネタバレでもあるので、目次だけ見て一度自分で作ってみてから進むのもよいかもしれません。
