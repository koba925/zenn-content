---
title: "2.7 カッコ"
---

カッコです。カッコの働きは標準で決まっている結合順や優先順を無視して、カッコが囲んでいる式を先に計算する、ということですね。

カッコを導入するためにあらたな AST を用意する必要はありません。たとえば `(2 + 3) * 4` と `2 + 3` を先に計算したいなら `("mul", [("add", [2, 3]), 4])` と書けばよいからです。

カッコに入った式は以下のように扱います。

`<プログラム>` は `<式>`、
・・・
`<プライマリ式>` は `<数>` または `(`＋ `<式>` ＋`)`

驚きがパターン化してますがこれだけです！あとはコードを書くだけ。

```diff py
                 ...
                 case c if c.isdecimal(): self._number()
-                case c if c in "+-*/%":
+                case c if c in "+-*/%()":
                     self._tokens.append(c); self._advance()
                 ...
```

字句解析が `(`・`)` のトークンを許すようにします。もうここは大丈夫ですね。

次がミソで、再帰下降の「再帰」がよくあらわれた部分です。

```diff py
 class Parser:
     ...

     def _primary(self):
         match self._current_token():
             case int(): return self._current_and_advance()
+            case "(": return self._group()
             case invalid:
                 assert False, f"Invalid token @ _primary(): {invalid}"
 
+    def _group(self):
+        self._current_and_advance()
+        expr = self._expression()
+        self._consume(")")
+        return expr

     def _binary_left(self, ops, sub_elem):
         ...
```

`(` というトークンに出会ったら次のトークンに進み、`<式>` をパースして、`)` で閉じていることを確かめます。`<プライマリ式>`は`<数>`または `(`＋ `<式>` ＋`)`、のとおりです。

ごく素直にカッコに囲まれた `<式>` を評価する、と書いただけですが、これでどれだけ複雑な入れ子になったカッコも処理できてしまいます。すごくは見えないのにすごいというところがすごいです（語彙）。再帰なしで書こうと思ったらまあまあ大変です。しらんけど。

`)` で閉じていることは `_consume()` で確かめます（consume は「消費する」という意味）。ここにはこのトークンが来ているはず、というチェックは今後もたくさん出てきます。すべて `_consume()` で確かめます。

```diff py
     def _binary_left(self, ops, sub_elem):
         ...
 
+    def _consume(self, expected):
+        assert self._current_token() == expected, \
+            f"Expected {expected} @ _consume(): {self._current_token()}"
+        return self._current_and_advance()

     def _current_token(self): return self._tokens[self._pos]
         ...
```

やってることはただ期待しているトークンかどうか確認して、次のトークンに進んでいるだけです。

実行例です。

```py
    print("Paren:")

    print(toil.ast(r""" (2 + 3) * 4 """))  # -> ('mul', [('add', [2, 3]), 4])
    print(toil.walk(r""" (2 + 3) * 4 """))  # -> 20
    print(toil.ast(r""" 2 * (3 + 4) """))  # -> ('mul', [2, ('add', [3, 4])])
    print(toil.walk(r""" 2 * (3 + 4) """))  # -> 14

    print(toil.walk(r""" (2 + 3) """))  # -> 5
    print(toil.walk(r""" (2) """))  # -> 2
    print(toil.walk(r""" (5 - (4 - 2)) * 2 """))  # -> 6
    print(toil.walk(r""" (2 + 3) * (4 + 5) """))  # -> 45

    # print(toil.walk(r""" 2 + 3) """))  # -> Extra token
    # print(toil.walk(r""" (+) """))  # -> Invalid token
    # print(toil.walk(r""" (2 + 3 """))  # -> Expected )
    # print(toil.walk(r""" () """))  # -> Invalid token
```

できてますね。単なる `2` も「式」なのでカッコで囲んだ `(2)` も正しい「式」です。

これで四則演算＋カッコの計算ができるようになりました。すでに再帰下降構文解析の基礎的なところはマスターしています。

次は数の計算からちょっと外れたところをやりましょう。

ソース：https://github.com/koba925/toil-book/blob/0207_paren/toil.py
差分：https://github.com/koba925/toil-book/compare/0206_mul_div_mod...0207_paren

## コラム：その他の構文解析手法

構文解析は昔からさかんに研究されてきた分野で、再帰下降以外にも多くの手法があり、新しい手法も開発されています。代表的なものを紹介します。

**Pratt パーサ**
再帰下降型構文解析器は作りやすいのですが、「掛け算は足し算より先」のような優先順位（Precedence）をまともに実装すると、関数定義が多段に重なり、実行時には再帰がとても深くなります。Pratt パーサでは優先順位をデータとして持たせ、ループで処理することによってこれを解決します。式と文のある言語では `2 + 3 * 4` のような「式」を Pratt パーサで、「式」を含む「文」を再帰下降で、という組み合わせが用いられます。データを使って表現しているので、優先度のカスタマイズなどがしやすいことも魅力です。
再帰下降の考え方をもとに、メソッドの組み合わせをデータで表現したもの、という感じもあるのでまずは再帰下降を理解してから挑戦してみるとよいと思います。Toil はすべて「式」なので、全部 Pratt パーサにすることも可能なはず・・・かな？

**LR 構文解析**
再帰下降がトップダウンに解析するのに対し、トークンをとりあえずスタックに積んでいき、まとまったら木の下から上に向かって（ボトムアップに）結合していく手法です。再帰下降よりも文法の自由度は上がりますが、構文解析を人間が手書きするのには向いておらず、Parser Generator（パーサジェネレータ）と呼ばれるツールを使って、文法定義ファイルから自動生成させるのが一般的な使い方です。本書では「原理を学ぶ」のが目的なのでツールや便利なライブラリは使わないようにしています。

**パーサコンビネータ（Parser Combinators）**
「数字を読むパーサ」「足し算を読むパーサ」といった小さな関数（パーサ）を作り、それらを組み合わせて言語のパーサにする手法です。関数型言語でよく使われます。根っこの考え方は再帰下降に似ています。

再帰下降は昔からある手法ですが今も現役です。単純に書きやすいというのもありますが、手書きだからこそ親切なエラーメッセージを出しやすい、というのも利点のひとつで、むしろ近年見直されつつある状況のようです。昔からある手法だから古くさい、なんてことはありませんのでご安心を。筆者自身は再帰下降のアルゴリズムに美しさを感じるくらいの感銘を受けていて、仮に古くさくて流行ってなかったとしても入門書では再帰下降を紹介するでしょう。
